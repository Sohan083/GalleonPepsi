<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/outlet_registration_nav_graph">
    <include
        app:graph="@navigation/logout_nav_graph"/>
    <include
        app:graph="@navigation/outlet_registration_nav_graph"/>
    <include
        app:graph="@navigation/consumer_contact_nav_graph"/>
    <include
        app:graph="@navigation/posm_activity_nav_graph"/>
    <include
        app:graph="@navigation/attendance_nav_graph"/>
    <include
        app:graph="@navigation/dashboard_nav_graph"/>
</navigation>